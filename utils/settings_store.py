def set_language(guild_id, language):
    """No-op placeholder kept for compatibility; the bot only uses English."""


def get_language(guild_id=None):
    """Always return English, since the bot no longer supports multiple languages."""
    return "en"
